# 2026-08-12

## SDatabase 模块（0.6.1）代码审查
- 离线 `mvn compile` 通过，无编译错误。
- 发现 6 项问题，详见 MEMORY.md「SDatabase 已知缺陷」：UPDATE/MERGE 上下文不可用、操作链无法执行、SELECT 列转义不一致、构造器 package-private 致文档示例不可编译、空 filter 恒 1=1、文档 MySQL 示例误用 root。
- 用户问「SdbDatabase 模块怎么用」时同步讲解了配置/三种执行方式/断言树/结果读取。
- SQL 注入防御复核：执行路径 buildPreparedSQL 中表名/SELECT 列/CTE 名曾裸拼（P0）。

## P0 修复：参数化执行路径标识符转义（2026-08-12 17:30）
- 修复 `SdbActionContext.buildPreparedSQL`：SELECT 投影列、DELETE/CREATE/ALTER/DROP/TRUNCATE/默认分支表名、WITH CTE 名全部改为 `SdbUtils.q()` 反引号转义（此前为裸拼接，保留字标识符报 SQL 错且动态标识符有注入面）。
- 已重新 `mvn -o compile` 通过（exit 0）。
- 更新 `docs/.change_log.md` 0.6.1 段，新增 P0 修复条目（原 SELECT 列条目重新归为「预览路径」）。
- 提交：git -c user.name=NeoNai -c user.email=neonai+coding@kdxiaoyi.top commit（仅 SdbActionContext.java + .change_log.md + 记忆文件）。
